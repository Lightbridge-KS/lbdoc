% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/md_to_df_str.R
\name{parse_md_to_df_str}
\alias{parse_md_to_df_str}
\title{Parse Markdown Content to a Structured DataFrame}
\usage{
parse_md_to_df_str(chr, summarize = TRUE, collapse = ", ")
}
\arguments{
\item{chr}{A character vector containing Markdown content, typically obtained
from \code{readLines()}.}

\item{summarize}{Logical, if \code{TRUE}, the content under each heading is summarized
into a single cell per heading level; if \code{FALSE}, each line retains its own row
in the output DataFrame.}

\item{collapse}{The string to use when collapsing multiple lines of content under
each heading when \code{summarize = TRUE}.}
}
\value{
A \code{data.frame} where each row corresponds to a heading or content line.
If \code{summarize} is \code{TRUE}, each heading level is summarized into single entries
per heading level. The DataFrame columns represent the hierarchical levels
(headings) and the content under these headings.
}
\description{
This function parses a vector of Markdown content into a structured DataFrame.
Each line of Markdown is analyzed for heading levels, and content under headings
is aggregated if specified. This allows for a tabular representation of hierarchical
document structures, useful for content analysis and manipulation.
}
\examples{
markdown_text <- c("# Heading 1", "Content under heading 1", "## Subheading 1.1",
"Content under subheading 1.1", "# Heading 2", "Content under heading 2")
md_df <- parse_md_to_df_str(markdown_text)
print(md_df)

}
